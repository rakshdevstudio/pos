import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder smoke test', (WidgetTester tester) async {
    // Placeholder — real integration tests to be added when backend is live.
    expect(true, isTrue);
  });
}

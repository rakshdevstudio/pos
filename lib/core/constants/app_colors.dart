import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF0A0A0A);
  static const Color surface = Color(0xFF141414);
  static const Color surfaceElevated = Color(0xFF1A1A1A);
  static const Color surfaceHighlight = Color(0xFF222222);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9E9E9E); // Grey
  static const Color textMuted = Color(0xFF757575);
  static const Color textDisabled = Color(0xFF424242);

  // Accent — Illume Gold
  static const Color accent = Color(0xFFD4AF37);
  static const Color accentDim = Color(0xFF8A7220);
  static const Color accentGlow = Color(0x33D4AF37);

  // Semantic
  static const Color success = Color(0xFF34D399);
  static const Color successDim = Color(0xFF064E3B);
  static const Color error = Color(0xFFEF4444);
  static const Color errorDim = Color(0xFF7F1D1D);
  static const Color warning = Color(0xFFFBBF24);

  // Borders
  static const Color border = Color(0xFF2A2A2A);
  static const Color borderFocus = Color(0xFFD4AF37);

  // Cart / Checkout
  static const Color cartBg = Color(0xFF141414);
  static const Color checkoutButton = Color(0xFFD4AF37);

  // Overlays
  static const Color overlay = Color(0xCC000000);
  static const Color shimmer = Color(0xFF2A2A2A);
  static const Color shimmerHighlight = Color(0xFF333333);
}

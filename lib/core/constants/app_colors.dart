import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF0B0B0B);
  static const Color surface = Color(0xFF1A1A1A);
  static const Color surfaceElevated = Color(0xFF222222);
  static const Color surfaceHighlight = Color(0xFF2C2C2C);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9CA3AF);
  static const Color textMuted = Color(0xFF6B7280);
  static const Color textDisabled = Color(0xFF4B5563);

  // Accent — soft gold
  static const Color accent = Color(0xFFD4AF6A);
  static const Color accentDim = Color(0xFF8A7245);
  static const Color accentGlow = Color(0x33D4AF6A);

  // Semantic
  static const Color success = Color(0xFF34D399);
  static const Color successDim = Color(0xFF064E3B);
  static const Color error = Color(0xFFF87171);
  static const Color errorDim = Color(0xFF7F1D1D);
  static const Color warning = Color(0xFFFBBF24);

  // Borders
  static const Color border = Color(0xFF2A2A2A);
  static const Color borderFocus = Color(0xFF3A3A3A);

  // Cart / Checkout
  static const Color cartBg = Color(0xFF111111);
  static const Color checkoutButton = Color(0xFFD4AF6A);

  // Overlays
  static const Color overlay = Color(0xCC000000);
  static const Color shimmer = Color(0xFF2A2A2A);
  static const Color shimmerHighlight = Color(0xFF333333);
}
